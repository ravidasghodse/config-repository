spring:
  application:
    name: naming-server
  config:
    import:
    - optional:configserver:http://localhost:8888
    name: naming-server
  cloud:
    config:
      profile: default
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false